#include "main.h"
/**
 * add - adds two integers and returns the result
 * @num1: number to be added to the sum
 * @num2: number to be added to the sum
 * Return: Always 0
 */

int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;

	return (sum);
}
