#include <stdio.h>
/**
 * _strlen - returns the length of a string
 * @s: length of a string
 * Return: Always 0
 */
int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}

	return (len);
}
